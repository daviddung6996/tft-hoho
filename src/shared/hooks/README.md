# Frontend Shared Hooks

Custom React hooks that are generic across the application.

## Allowed:
- useWindowSize.ts
- useDebounce.ts
- useLocalStorage.ts

## Disallowed:
- `useAuth()` (belongs in `src/features/auth/`)
- `useFetchMatches()` (belongs in `src/features/matches/`)
