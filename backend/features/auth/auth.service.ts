export class AuthService {
    // Logic core for authentication
    public async processLogin(credentials: any) {
        // 1. Verify credentials
        // 2. Generate token
        // 3. Return user & token
    }
}
